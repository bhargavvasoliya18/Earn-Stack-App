import 'package:intl/intl.dart';

var outputFormat = DateFormat('dd/MM/yyyy');
var outputMonthFormat = DateFormat('dd MMM yyyy');
var calendarDateFormat = DateFormat('yyyy-MM-dd');
var yearMonthDayDateFormat = DateFormat('yyyy-MM-dd');
var hourMinute = DateFormat('HH:mm');
var monthFormat = DateFormat("MM");
var yearFormat = DateFormat("yyyy");